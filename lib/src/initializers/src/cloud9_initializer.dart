/// An abstract class representing the initialization logic for a cloud storage service.
// ignore_for_file: one_member_abstracts

abstract class Cloud9Initializer {
  /// Initializes the cloud storage service.
  void initialize();
}
